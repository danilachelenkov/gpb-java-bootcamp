# 📦 Модуль 3: Database Schema — проектирование БД

## 🎯 Цель модуля
Спроектировать и реализовать полную схему БД для P2P системы платежей с использованием Liquibase миграций.

## 📋 Бизнес-процесс: P2P Переводы

**Суть системы:**

- Каждый пользователь может создать один или несколько кошельков (`Wallet`) в разных валютах.
- Пользователь переводит деньги со своего кошелька на кошелек другого пользователя.
- Каждый перевод создает транзакцию с историей всех операций и статусами.
- На переводы могут быть наложены лимиты (макс. сумма, макс. количество в день).
- Все операции атомарны — если что-то пошло не так, деньги не теряются.

### Пример сценария:

- Иван создал кошельки: `RUB wallet` (5000₽), `USD wallet` (100$)
- Петя создал кошельки: `RUB wallet` (1000₽)

> Иван отправляет Пете 500₽  
> → Транзакция: Иван RUB **-500₽** → Петя RUB **+500₽**  
> → История сохраняется с `timestamp` и статусом `COMPLETED`

## 🗄️ Таблицы и атрибуты

### 1️⃣ `users` — Пользователи

| Атрибут         | Тип                              | Описание                          |
|------------------|----------------------------------|-----------------------------------|
| `id`            | `BIGSERIAL PRIMARY KEY`         | Уникальный ID                     |
| `email`         | `VARCHAR(255) UNIQUE NOT NULL`  | Email (логин)                     |
| `password_hash` | `VARCHAR(255) NOT NULL`         | Хеш пароля                        |
| `first_name`    | `VARCHAR(100)`                  | Имя                               |
| `last_name`     | `VARCHAR(100)`                  | Фамилия                           |
| `status`        | `ENUM ('ACTIVE', 'BLOCKED', 'DELETED')` | Статус аккаунта            |
| `created_at`    | `TIMESTAMP DEFAULT NOW()`       | Дата создания                     |
| `updated_at`    | `TIMESTAMP DEFAULT NOW()`       | Дата обновления                   |
| `deleted_at`    | `TIMESTAMP NULL`                | Soft delete                       |

**Индексы:**

- `UNIQUE INDEX idx_users_email`
- `INDEX idx_users_status`

### 2️⃣ `wallets` — Кошельки пользователей

| Атрибут         | Тип                              | Описание                          |
|------------------|----------------------------------|-----------------------------------|
| `id`            | `BIGSERIAL PRIMARY KEY`         | Уникальный ID                     |
| `user_id`       | `BIGINT NOT NULL`               | Ссылка на пользователя            |
| `currency`      | `VARCHAR(3) NOT NULL`           | Код валюты (RUB, USD, EUR)        |
| `balance`       | `DECIMAL(19,2) NOT NULL DEFAULT 0` | Баланс (**НЕ FLOAT!**)         |
| `wallet_number` | `VARCHAR(30) UNIQUE NOT NULL`   | Номер кошелька                    |
| `status`        | `ENUM ('ACTIVE', 'FROZEN', 'CLOSED')` | Статус кошелька             |
| `created_at`    | `TIMESTAMP DEFAULT NOW()`       | Дата создания                     |
| `updated_at`    | `TIMESTAMP DEFAULT NOW()`       | Дата обновления                   |

**Constraints:**

- `FOREIGN KEY (user_id) REFERENCES users(id)`
- `CHECK (balance >= 0)` — баланс не может быть отрицательным
- `UNIQUE (user_id, currency)` — один кошелек на валюту

**Индексы:**

- `INDEX idx_wallets_user_id`
- `INDEX idx_wallets_currency`
- `INDEX idx_wallets_status`

---

### 3️⃣ `transactions` — История переводов

| Атрибут            | Тип                              | Описание                          |
|---------------------|----------------------------------|-----------------------------------|
| `id`               | `BIGSERIAL PRIMARY KEY`         | Уникальный ID                     |
| `from_wallet_id`   | `BIGINT NOT NULL`               | Кошелек отправителя               |
| `to_wallet_id`     | `BIGINT NOT NULL`               | Кошелек получателя                |
| `amount`           | `DECIMAL(19,2) NOT NULL`        | Сумма перевода                    |
| `currency`         | `VARCHAR(3) NOT NULL`           | Валюта                            |
| `type`             | `ENUM ('TRANSFER', 'DEPOSIT', 'WITHDRAWAL')` | Тип операции           |
| `status`           | `ENUM ('PENDING', 'COMPLETED', 'FAILED', 'CANCELLED')` | Статус      |
| `description`      | `TEXT`                          | Описание/комментарий              |
| `idempotency_key`  | `VARCHAR(255) UNIQUE`           | Ключ идемпотентности (защита от дубликатов) |
| `created_at`       | `TIMESTAMP DEFAULT NOW()`       | Дата создания                     |
| `completed_at`     | `TIMESTAMP NULL`                | Дата завершения                   |

**Constraints:**

- `FOREIGN KEY (from_wallet_id) REFERENCES wallets(id)`
- `FOREIGN KEY (to_wallet_id) REFERENCES wallets(id)`
- `CHECK (amount > 0)` — сумма должна быть положительной

**Индексы:**

- `INDEX idx_transactions_from_wallet_id`
- `INDEX idx_transactions_to_wallet_id`
- `INDEX idx_transactions_status`
- `INDEX idx_transactions_created_at`
- `UNIQUE INDEX idx_transactions_idempotency_key`

---

### 4️⃣ `transfer_limits` — Лимиты на переводы

| Атрибут                | Тип                              | Описание                          |
|------------------------|----------------------------------|-----------------------------------|
| `id`                  | `BIGSERIAL PRIMARY KEY`         | Уникальный ID                     |
| `wallet_id`           | `BIGINT NOT NULL`               | Кошелек                           |
| `max_single_transfer` | `DECIMAL(19,2)`                 | Макс. размер одного перевода      |
| `max_daily_amount`    | `DECIMAL(19,2)`                 | Макс. сумма в день                |
| `max_daily_count`     | `INTEGER`                       | Макс. кол-во переводов в день     |
| `created_at`          | `TIMESTAMP DEFAULT NOW()`       | Дата создания                     |
| `updated_at`          | `TIMESTAMP DEFAULT NOW()`       | Дата обновления                   |

**Constraints:**

- `FOREIGN KEY (wallet_id) REFERENCES wallets(id) ON DELETE CASCADE`
- `UNIQUE (wallet_id)`

**Индексы:**

- `INDEX idx_transfer_limits_wallet_id`

## 📝 Задания

### Задание 1: Создать миграцию для таблицы `users`

**Файл:** `src/main/resources/db/changelog/001_create_users_table.yaml`

**Требование:** Создать корректный YAML с **ВСЕМИ** колонками из таблицы.

```yaml
databaseChangeLog:
  - changeSet:
      id: 001
      author: dev
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: BIGSERIAL
                  constraints:
                    primaryKey: true
              - column:
                  name: email
                  type: VARCHAR(255)
                  constraints:
                    nullable: false
                    unique: true
                    # ... остальные колонки
              
        - createIndex:
            tableName: users
            indexName: idx_users_email
            unique: true
            columns:
              - column:
                  name: email
```

### Требование
Создать корректный YAML с **ВСЕМИ** колонками из таблицы.

---

### Задание 2: Создать миграции для таблиц `wallets` и `transactions`

**Файлы:**

- `src/main/resources/db/changelog/002_create_wallets_table.yaml`
- `src/main/resources/db/changelog/003_create_transactions_table.yaml`

**Требования:**

- Добавить `FOREIGN KEY` constraints  
- Добавить `CHECK` constraints (`balance >= 0`, `amount > 0`)  
- Создать все необходимые индексы  
- Использовать `DECIMAL(19,2)` для денежных сумм

---

### Задание 3: Создать миграцию для таблицы `transfer_limits`

**Файл:**  
`src/main/resources/db/changelog/004_create_transfer_limits_table.yaml`

**Требования:**

- Ссылка на таблицу `wallets` с `ON DELETE CASCADE`  
- `UNIQUE` constraint на `wallet_id`  
- Все колонки из схемы выше

---

### Задание 4: Проверить применение миграций

**Запустить приложение:**
```bash
./gradlew bootRun
```

### Проверка после запуска

#### 1. Проверить в DBeaver / DataGrip:

- Все **4 таблицы** (`users`, `wallets`, `transactions`, `transfer_limits`) созданы в PostgreSQL  
- **Структура таблиц** полностью соответствует описанной схеме (типы колонок, ограничения, значения по умолчанию)  
- Все требуемые **индексы созданы** (`idx_users_email`, `idx_wallets_user_id`, `idx_transactions_idempotency_key` и т.д.)

#### 2. Проверить в логах приложения:

Убедитесь, что в консоли или лог-файле присутствуют следующие строки (или аналогичные):

```
... Liquibase: Successfully executed update for changeSet db/changelog/001_...
... Liquibase: Successfully executed update for changeSet db/changelog/002_...
... Liquibase: Successfully executed update for changeSet db/changelog/003_...
... Liquibase: Successfully executed update for changeSet db/changelog/004_...
```
### Задание 5: Создать тестовые данные (Seed миграция)

**Файл:**  
`src/main/resources/db/changelog/005_seed_test_data.yaml`

**Содержание:**

```yaml
- insert: # Вставить 2 тестовых пользователя (ivan@test.com, petr@test.com)
- insert: # Вставить 3 кошелька (2 на Ивана, 1 на Петра)
- insert: # Вставить 2 транзакции (примеры переводов)
```
### ✅ Чек-лист успешного прохождения

- [ ] 4 миграции созданы (`users`, `wallets`, `transactions`, `transfer_limits`)
- [ ] Все таблицы видны в DBeaver / DataGrip
- [ ] Структура совпадает со схемой выше (колонки, типы, constraints)
- [ ] Индексы созданы (проверка: `\di` в `psql`)
- [ ] `CHECK` constraints работают (попытка `INSERT` с отрицательным `balance` → ошибка)
- [ ] `FOREIGN KEY` constraints работают (удаление юзера → ошибка, если есть кошельки)
- [ ] Seed данные загружены (в таблицах есть 2+ записи)
- [ ] Liquibase история записана (таблица `databasechangelog`)
- [ ] Приложение стартует без ошибок после запуска миграций
- [ ] Git коммит с миграциями отправлен на GitHub

### 🎓 Что освоишь

- ✅ Проектирование БД для реальных систем  
- ✅ `DECIMAL` vs `FLOAT` для денежных операций  
- ✅ Constraints, индексы, FK для целостности  
- ✅ Миграции через Liquibase (версионирование БД)  
- ✅ Типы данных PostgreSQL  

**Время выполнения:** 2–3 часа