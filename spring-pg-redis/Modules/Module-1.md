## 📦 Модуль 1: Подготовка окружения и инфраструктура

### 🎯 Цель модуля
Настроить локальное окружение для разработки: установить Docker, запустить PostgreSQL и Redis в контейнерах, подключиться к ним через графические инструменты и создать GitHub репозиторий для проекта.

**Результат:** У тебя будет работающая инфраструктура (PostgreSQL + Redis), к которой ты сможешь подключаться из Spring Boot приложения.

### ⏱️ Время выполнения: 2-3 часа

### 📋 Чек-лист модуля
- [ ] Docker Desktop установлен и запущен
- [ ] PostgreSQL контейнер работает на порту 5432
- [ ] Redis контейнер работает на порту 6379
- [ ] DBeaver/DataGrip подключен к PostgreSQL
- [ ] Redis Insight установлен и подключен к Redis
- [ ] GitHub репозиторий создан
- [ ] Структура папок проекта готова
- [ ] `.gitignore` настроен

## 🛠️ Задание 1: Установка Docker Desktop

### Шаг 1.1: Скачивание Docker
1. Перейди на официальный сайт: [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
2. Скачай версию для твоей ОС (Windows/Mac/Linux)
3. Запусти установщик

### Шаг 1.2: Установка

#### Windows:
1. Убедись, что включен **WSL 2** (Windows Subsystem for Linux)
2. Следуй инструкциям установщика
3. После установки перезагрузи компьютер

### Шаг 1.3: Проверка установки
Открой терминал (Windows: PowerShell или CMD, Mac/Linux: Terminal) и выполни:

```
docker --version
```

Ожидаемый результат:
```
Docker version 24.0.7, build afdd53b
```

✅ **Критерий выполнения:**
Docker Desktop запущен, в трее (системном лотке) видна иконка Docker. Команды `docker --version` и `docker compose version` выводят версии.

## 🐘 Задание 2: Запуск PostgreSQL через Docker

### Шаг 2.1: Создание папки проекта
Создай папку для проекта:

(Можно и руками создать через проводник)

**Windows:**
```cmd

mkdir p2p-payments-project-chelenkov (#пишем свою фамилию, чтобы различать проекты)
cd p2p-payments-project-chelenkov
```

### Шаг 2.2: Создание docker-compose.yml
Создай файл docker-compose.yml в корне папки проекта со следующим содержимым:

```yaml
version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: p2p-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: p2p_payment_db
      POSTGRES_USER: p2p_user
      POSTGRES_PASSWORD: p2p_password
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U p2p_user -d p2p_payment_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: p2p-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass redis_password
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
```
### Шаг 2.3: Запуск контейнеров
В терминале (находясь в папке проекта) выполни:
```bash
docker compose up -d
```
Флаг -d означает "detached mode" — контейнеры запустятся в фоне.

Ожидаемый вывод:
```
[+] Running 4/4
 ✔ Network p2p-payment-system_default    Created
 ✔ Volume "p2p-payment-system_postgres_data"  Created
 ✔ Volume "p2p-payment-system_redis_data"     Created
 ✔ Container p2p-postgres                Started
 ✔ Container p2p-redis                   Started
```

### Шаг 2.4: Проверка статуса контейнеров
```bash
docker compose ps
```
Ожидаемый результат:
```
NAME            IMAGE                COMMAND                  STATUS         PORTS
p2p-postgres    postgres:15-alpine   "docker-entrypoint.s…"   Up 10 seconds  0.0.0.0:5432->5432/tcp
p2p-redis       redis:7-alpine       "redis-server --appe…"   Up 10 seconds  0.0.0.0:6379->6379/tcp
```
Статус должен быть: Up (работает) или Up (healthy) (работает и прошел healthcheck).

### Шаг 2.5: Просмотр логов (опционально)
Если хочешь посмотреть что происходит внутри контейнеров:

```bash
# Логи PostgreSQL
docker compose logs postgres

# Логи Redis
docker compose logs redis

# Логи в режиме реального времени (Ctrl+C для выхода)
docker compose logs -f
```

✅ Критерий выполнения:
Команда docker compose ps показывает оба контейнера в статусе Up. Логи не содержат ошибок (ERROR).

## 🔌 Задание 3: Подключение к PostgreSQL через DBeaver

### Шаг 3.1: Установка DBeaver

#### Вариант А: DBeaver Community (бесплатный)
1. Скачай с сайта: [https://dbeaver.io/download/](https://dbeaver.io/download/)
2. Установи согласно инструкциям для твоей ОС

#### Вариант Б: DataGrip (платный, но есть trial)
1. Скачай: [https://www.jetbrains.com/datagrip/](https://www.jetbrains.com/datagrip/)
2. Используй student license (если ты студент) или trial версию

#### Вариант В: Командная строка (psql)
(Опционально, если хочешь использовать CLI)

```bash
# Подключение через Docker
docker exec -it p2p-postgres psql -U p2p_user -d p2p_payment_db
```

### Шаг 3.2: Создание подключения в DBeaver
1. Открой DBeaver
2. Нажми **Database → New Database Connection**
3. Выбери **PostgreSQL**
4. Нажми **Next**

### Шаг 3.3: Настройка параметров подключения
Введи следующие данные:

| Параметр | Значение |
|----------|----------|
| Host | localhost |
| Port | 5432 |
| Database | p2p_payment_db |
| Username | p2p_user |
| Password | p2p_password |

Поставь галочку **"Save password"**.

### Шаг 3.4: Тестирование подключения
1. Нажми кнопку **Test Connection**
2. При первом подключении DBeaver предложит скачать драйвер PostgreSQL — соглашайся (**Download**)

**Ожидаемый результат:** Подключение успешно установлено (Status: Connected).
```
Connected
```

**Если видишь ошибку — проверь:**
- Запущен ли контейнер (`docker compose ps`)
- Правильно ли указаны `host/port/credentials`
- Не занят ли порт 5432 другим приложением

### Шаг 3.5: Проверка БД
1. Нажми **Finish**
2. В левой панели разверни: `p2p_payment_db` → `Databases` → `p2p_payment_db` → `Schemas` → `public` → `Tables`
3. Сейчас там пусто — это нормально, таблицы мы создадим позже через Liquibase

✅ **Критерий выполнения:**
DBeaver подключился к базе, видна структура БД (пока без таблиц).

## 🔴 Задание 4: Подключение к Redis через Redis Insight

### Шаг 4.1: Установка Redis Insight
1. Перейди на сайт: [https://redis.io/insight/](https://redis.io/insight/)
2. Скачай версию для твоей ОС (Windows/Mac/Linux)
3. Установи приложение

**Альтернатива (Docker версия):**
(Опционально, если хочешь запустить Redis Insight в контейнере)

```bash
docker run -d --name redis-insight \
  -p 5540:5540 \
  redis/redisinsight:latest
```
Затем открой: `http://localhost:5540`

### Шаг 4.2: Добавление подключения
1. Открой Redis Insight
2. Нажми **+ Add Redis Database**
3. Выбери **Connect to a Redis Database**

### Шаг 4.3: Настройка параметров
Введи следующие данные:

| Параметр | Значение |
|----------|----------|
| Host | localhost |
| Port | 6379 |
| Database Alias | p2p-redis-local |
| Username | (оставь пустым) |
| Password | redis_password |

### Шаг 4.4: Тестирование подключения
1. Нажми **Add Redis Database**
2. Если все правильно — увидишь подключение в списке

### Шаг 4.5: Проверка работы Redis
1. Кликни на созданное подключение
2. Перейди на вкладку **Browser**
3. Нажми **+ Add Key** → **String**
4. Создай ключ:
   - **Key name:** `test:hello`
   - **Value:** `world`
   - **TTL:** `60` (время жизни 60 секунд)
5. Нажми **Add Key**
6. Теперь в списке ключей появится `test:hello` со значением `world`.
7. Подожди 60 секунд и обнови страницу — ключ исчезнет (TTL истек).

✅ **Критерий выполнения:**
Redis Insight подключился, можешь создавать/просматривать/удалять ключи. Видишь как работает TTL.

## 🐙 Задание 5: Создание GitHub репозитория

### Шаг 5.1: Создание репозитория на GitHub
1. Перейди на [https://github.com](https://github.com)
2. Войди в свой аккаунт (или создай новый)
3. Нажми **+** (плюс) в правом верхнем углу → **New repository**

### Шаг 5.2: Настройка репозитория
Заполни форму:
| Поле | Значение |
|------|----------|
| Repository name | p2p-payment-system |
| Description | P2P payment system with Spring Boot, PostgreSQL and Redis |
| Visibility | Public (для портфолио) |
| Initialize this repository with: | ❌ **Не ставь галочки** (репозиторий должен быть пустым) |

Нажми **Create repository**.

### Шаг 5.3: Инициализация Git локально
Вернись в терминал (в папке проекта):

```bash
# Инициализация Git
git init

# Проверка статуса
git status
```

### Шаг 5.4: Создание .gitignore
Создай файл .gitignore в корне проекта со следующим содержимым:

```gitignore
# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr
out/

# Eclipse
.classpath
.project
.settings/
bin/

# VS Code
.vscode/

# MacOS
.DS_Store

# Logs
logs/
*.log

# Environment variables
.env
.env.local

# Spring Boot
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

# Temporary files
*.tmp
*.bak
*.swp
*~.nib
```

### Шаг 5.5: Первый коммит
```bash
# Добавить все файлы в staging
git add .

# Создать коммит
git commit -m "Initial commit: Docker infrastructure setup"

# Указать ветку main (если у тебя master — переименуй)
git branch -M main

# Добавить remote репозиторий (замени YOUR_USERNAME на свой)
git remote add origin https://github.com/YOUR_USERNAME/p2p-payment-system.git

# Отправить на GitHub
git push -u origin main
```

**Если GitHub попросит аутентификацию:**

- Используй Personal Access Token вместо пароля
- Создай token: GitHub → Settings → Developer settings → Personal access tokens → - Generate new token
- Выбери scope: repo (полный доступ к репозиториям)

✅ Критерий выполнения:
На GitHub в твоем репозитории видны файлы docker-compose.yml и .gitignore. Commit отображается в истории.

## 📁 Задание 6: Создание структуры папок проекта

### Шаг 6.1: Создание базовой структуры
В корне проекта создай следующие папки и файлы:

```
p2p-payment-system/
├── docker/
│ ├── postgres/
│ │ └── init.sql
│ └── redis/
│ └── redis.conf
├── docker-compose.yml
├── .gitignore
├── README.md
└── docs/
└── architecture.md
```

```bash
# Windows (PowerShell)
New-Item -ItemType Directory -Path "docs"
New-Item -ItemType File -Path "docs\README.md"
New-Item -ItemType File -Path "README.md"

# Mac/Linux
mkdir -p docs
touch docs/README.md
touch README.md
```
**Проверка:**

Выполни команду ls -la (Mac/Linux) или dir (Windows) в папке проекта — должна отобразиться созданная структура.

✅ Критерий выполнения:
Все папки и файлы из схемы выше созданы в корне проекта.

### Шаг 6.2: Заполнение README.md
Открой файл `README.md` в корне проекта и добавь следующее содержимое:
```markdown
# 💳 P2P Payment System

Учебный проект — система peer-to-peer платежей на Spring Boot с Redis кэшированием.

## 🚀 Технологии

- **Backend:** Java 21, Spring Boot 3.2
- **Database:** PostgreSQL 15
- **Cache:** Redis 7
- **Build Tool:** Gradle
- **Containerization:** Docker, Docker Compose

## 📦 Запуск инфраструктуры

```bash
# Запуск PostgreSQL + Redis
docker compose up -d

# Проверка статуса
docker compose ps

# Остановка
docker compose down
```

## 🔌 Подключения

### PostgreSQL
- **Host:** `localhost:5432`
- **Database:** `p2p_payment_db`
- **User:** `p2p_user`
- **Password:** `p2p_password`

### Redis
- **Host:** `localhost:6379`
- **Password:** `redis_password`

## 📚 Документация
Подробная документация модулей находится в папке `/docs`.

## 👨‍💻 Автор
[Твое имя]

### **✅ Критерий выполнения:**

Структура папок создана, README.md заполнен базовой информацией о проекте.

## 🧪 Задание 7: Финальная проверка инфраструктуры

### **Шаг 7.1: Проверочный чек-лист**

Выполни все команды и убедись, что результаты совпадают:

#### **1. Docker контейнеры запущены**
```bash
docker compose ps
```

✅ Оба контейнера в статусе Up или Up (healthy)

#### **2. PostgreSQL доступен**
```bash
docker exec -it p2p-postgres psql -U p2p_user -d p2p_payment_db -c "SELECT version();"
```
✅ Видишь версию PostgreSQL (например: PostgreSQL 15.5)

#### **3. Redis доступен**
```bash
docker exec -it p2p-redis redis-cli -a redis_password PING
```

### **4. DBeaver подключен**
Открой DBeaver
Кликни на подключение p2p_payment_db
Выполни SQL запрос:
```sql
SELECT current_database(), current_user;
```
✅ Видишь результат: p2p_payment_db | p2p_user

#### **5. Redis Insight подключен**
- Открой Redis Insight
- Перейди в Browser
- Выполни команду в CLI:
```redis
SET test:module1 "completed"
GET test:module1
```
✅ Видишь значение: "completed"

#### **6. GitHub репозиторий обновлен**

```bash
git status
```

✅ Должно быть: nothing to commit, working tree clean

Если есть незакоммиченные файлы:
```bash
git add .
git commit -m "docs: Add project README and structure"
git push
```

### Шаг 7.2: Создание скриншотов (для портфолио)

Сделай скриншоты следующих элементов и сохрани их в папке:
```
docs/screenshots/module-01/
```
**Обязательные скриншоты:**

1. **Docker Desktop** — список запущенных контейнеров (`docker compose ps`)  
   → Должны быть видны `postgres` и `redis` в статусе `Up`

2. **DBeaver** — успешное подключение к PostgreSQL  
   → Видно подключение к `p2p_payment_db`, развернутая структура `public` (даже если таблиц пусто)

3. **Redis Insight** — интерфейс с ключом `test:module1`  
   → Виден ключ `test:module1` со значением `success` (или `hello`) и TTL > 0

4. **GitHub** — репозиторий с файлами проекта  
   → Видна структура папок: `docker/`, `.gitignore`, `README.md`, `docker-compose.yml`

5. **(Опционально, но рекомендуется)** — **PostgreSQL в DBeaver**: открыта вкладка `Schemas → public → Tables` (пусто, но структура доступна)

6. **(Опционально, но рекомендуется)** — **Redis Insight**: вкладка `Browser` с фильтром по ключу `test:*`

> 💡 **Совет:** Используйте названия файлов для удобства:  
> - `docker-containers.png`  
> - `dbeaver-connection.png`  
> - `redis-insight-key.png`  
> - `github-repo-structure.png`  
> - `pg-schemas.png`  
> - `redis-browser.png`

✅ **Критерий выполнения:**  
Все 6 проверок пройдены успешно.  
Скриншоты сохранены в папке `docs/screenshots/module-01/`.

## 🎯 Итоговый чек-лист модуля

Перед переходом к Модулю 2 убедись, что всё выполнено:

- [✅] Docker Desktop установлен и работает  
- [✅] `docker compose ps` показывает оба контейнера в статусе `Up`  
- [✅] PostgreSQL доступен на `localhost:5432`  
- [✅] Redis доступен на `localhost:6379`  
- [✅] DBeaver / DataGrip успешно подключен к PostgreSQL  
- [✅] Redis Insight успешно подключен к Redis  
- [✅] Создан и отправлен тестовый ключ в Redis (проверил работу TTL)  
- [✅] GitHub репозиторий создан и содержит:  
  - `docker-compose.yml`  
  - `.gitignore`  
  - `README.md`  
- [✅] Структура папок `/docs` создана  
- [✅] Все команды проверки (Задание 7.1) выполнены успешно  

✅ **Готов к следующему модулю!** Переходи к **Модуль 2: Spring Boot проект — создание и настройка**.

