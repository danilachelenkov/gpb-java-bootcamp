# Типы данных в Redis

Redis — это не просто хранилище ключ-значение. Он поддерживает **разные типы значений**, что делает его гибким инструментом для решения различных задач: от кэширования до анализа временных рядов.

---

## 🔢 Основные (встроенные) типы данных

### 1. **String (Строка)** — *самый простой тип*

- **Что это?**  
  Просто последовательность байтов — как строка в Java или Python.  
  Может хранить текст, JSON, XML, даже бинарные данные (например, изображение).

- **Особенности:**
  - Максимальный размер — **512 МБ**.
  - Можно хранить числа как строки — Redis умеет их инкрементировать (`INCR`, `DECR`).
  - Поддерживает операции: `SET`, `GET`, `INCR`, `DECR`, `APPEND`, `STRLEN`.

- **Когда использовать?**
  - Хранение сессий пользователя: `session:12345 = "user_id=789"`
  - Кэширование HTML-страницы
  - Счётчики: `views:article:100 = 456`
  - Хранение конфигов в виде JSON: `config:app = "{\"timeout\":30}"`

> ✅ **Пример:**  
> `SET user:1001 name "Иван"`  
> `GET user:1001` → `"Иван"`

---

### 2. **Hash (Хеш, словарь)** — *объект с полями и значениями*

- **Что это?**  
  Как будто у вас есть объект в Java: `Map<String, String>`.  
  Один ключ хранит **несколько полей (fields)**, каждое со своим значением.

- **Особенности:**
  - Очень эффективен для хранения объектов (например, пользователь, товар).
  - Не нужно читать весь объект целиком — можно получить только одно поле.
  - Экономит память, если много похожих объектов.

- **Когда использовать?**
  - Хранение профиля пользователя:  
    `user:1001` → `{name: "Иван", email: "ivan@example.com", age: "30"}`
  - Хранение информации о товаре: `product:2001` → `{name: "Книга", price: "999", stock: "15"}`

- **Команды:** `HSET`, `HGET`, `HMSET`, `HGETALL`, `HDEL`

> ✅ **Пример:**  
> `HSET user:1001 name "Иван" email "ivan@example.com" age "30"`  
> `HGET user:1001 name` → `"Иван"`  
> `HGETALL user:1001` → вернёт все поля сразу

> 💡 **Плюс:** Если у вас 10 000 пользователей — каждый как Hash, Redis хранит их компактно.  
> Если бы вы хранили каждого как отдельную строку — было бы неэффективно.

---

### 3. **List (Список)** — *упорядоченная коллекция элементов*

- **Что это?**  
  Как массив или список в Java — элементы идут в порядке добавления.  
  Можно добавлять в начало и в конец.  
  Допускаются **дубликаты**.

- **Особенности:**
  - Оптимизирован для операций на концах списка (`LEFT`, `RIGHT`).
  - Не подходит для быстрого доступа по индексу (если список большой — медленно).
  - Часто используется как очередь.

- **Когда использовать?**
  - Очередь задач (например, отправка писем):  
    `queue:email:send` — добавляете письма в конец, забираете с начала.
  - Лента новостей: `posts:user:1001` — последние посты в порядке добавления.
  - Логи событий.

- **Команды:** `LPUSH`, `RPUSH`, `LPOP`, `RPOP`, `LRANGE`, `LLEN`

> ✅ **Пример:**  
> `LPUSH posts:1001 "Пост 1"`  
> `LPUSH posts:1001 "Пост 2"`  
> `LRANGE posts:1001 0 -1` → вернёт: `["Пост 2", "Пост 1"]`  
> (т.к. добавляли в начало — последнее добавленное первое)

---

### 4. **Set (Множество)** — *набор уникальных элементов, без порядка*

- **Что это?**  
  Как `Set` в Java — **только уникальные элементы**, порядок **не гарантируется**.

- **Особенности:**
  - Автоматически удаляет дубликаты.
  - Очень быстрые операции проверки наличия элемента (`SISMEMBER`).
  - Поддерживает математические операции: пересечение, объединение, разность.

- **Когда использовать?**
  - Подписки пользователей на теги: `tags:java:subscribers` → `{user1, user2, user3}`
  - Уникальные посещения страницы: `visitors:article:1001` → хранит ID пользователей, которые заходили (без дублей).
  - Рекомендации: "пользователи, которые смотрели это, смотрели и то" — через пересечение множеств.

- **Команды:** `SADD`, `SMEMBERS`, `SISMEMBER`, `SINTER`, `SUNION`, `SDIFF`

> ✅ **Пример:**  
> `SADD followers:1001 "user2" "user3" "user2"`  
> → `SMEMBERS followers:1001` → `["user2", "user3"]` (дубль "user2" удалён)

---

### 5. **Sorted Set (Отсортированное множество)** — *множество с весом (score)*

- **Что это?**  
  Как `Set`, но каждый элемент имеет **числовой вес — score**.  
  Элементы автоматически сортируются по этому score.

- **Особенности:**
  - Уникальные элементы (как в Set).
  - Сортировка по score — от меньшего к большему.
  - Можно быстро получить топ-N элементов.
  - Можно менять score позже — и сортировка обновится автоматически.

- **Когда использовать?**
  - Топ-10 рейтингов: `leaderboard:game` → `{user1: 9500, user2: 8700, ...}`
  - Очередь с приоритетом: `tasks:priority` → `{task1: 5, task2: 10}` — чем выше score, тем выше приоритет.
  - Кэш с TTL: можно хранить время жизни как score и находить устаревшие элементы.

- **Команды:** `ZADD`, `ZRANGE`, `ZREVRANGE`, `ZRANK`, `ZSCORE`, `ZREM`

> ✅ **Пример:**  
> `ZADD leaderboard:game 9500 "user1" 8700 "user2" 10200 "user3"`  
> `ZREVRANGE leaderboard:game 0 2 WITHSCORES` →  
> `["user3", "10200", "user1", "9500", "user2", "8700"]`  
> (топ-3 по убыванию)

---

## 🧩 Дополнительные (модульные) типы данных

> 💡 Эти типы **не входят в стандартный Redis**, но доступны как **Redis-модули**.  
> Их можно подключить к серверу Redis или использовать в **Redis Stack**.

### 6. **RedisJSON** — *хранение и работа с JSON-документами*

- **Что это?**  
  Модуль, позволяющий хранить **JSON-объекты** как значения и работать с ними:  
  читать, изменять, обновлять **внутри JSON** — без необходимости загружать весь объект.

- **Особенности:**
  - Поддержка JSON-путей (как в `jq` или `XPath`).
  - Позволяет обновлять только часть JSON, а не весь объект.
  - Интегрирован с другими Redis-типами (например, можно хранить JSON в Hash-полях).
  - Использует стандарт JSONPath.

- **Когда использовать?**
  - Хранение сложных объектов: профиль пользователя, настройки, товары.
  - Когда в JSON много вложенных данных, и вы не хотите загружать всё целиком.
  - Удобно для микросервисов, передающих JSON.

- **Команды:** `JSON.SET`, `JSON.GET`, `JSON.NUMINCRBY`, `JSON.ARRAPPEND`, `JSON.TOGGLE`, и т.д.

> ✅ **Пример:**  
> `JSON.SET user:1001 $ {"name":"Иван", "age":30, "city":"Москва"}`  
> `JSON.GET user:1001 $.name` → `"Иван"`  
> `JSON.NUMINCRBY user:1001 $.age 1` → возраст стал 31

---

### 7. **Redis Streams** — *лог событий с поддержкой потребителей (consumer groups)*

- **Что это?**  
  Как очередь сообщений, но более продвинутая.  
  Позволяет хранить **события** с метками времени, читать их по ID, использовать **группы потребителей**, подтверждение обработки.

- **Особенности:**
  - Подходит для построения **event-driven архитектур**.
  - Встроенные **группы потребителей** (consumer groups) — как в Kafka.
  - Автоматическая нумерация событий по времени.
  - Возможность "читать с определённого момента".

- **Когда использовать?**
  - Аудит действий пользователя: `stream:audit` → `[{"action": "login", "user": "1001"}, ...]`
  - Логирование событий: `stream:logs`
  - Обработка задач в очереди с подтверждением: `stream:tasks`

- **Команды:** `XADD`, `XREAD`, `XGROUP`, `XACK`, `XRANGE`, `XREADGROUP`

> ✅ **Пример:**  
> `XADD mystream * username Alice action login`  
> → вернёт ID события: `1635123456789-0`  
> `XREAD COUNT 1 STREAMS mystream $` → читает с конца

---

### 8. **RedisTimeSeries (RedisTS)** — *хранение и анализ временных рядов*

- **Что это?**  
  Специализированный тип для хранения **временных меток и значений**.  
  Подходит для хранения данных, которые меняются со временем: метрики, датчики, статистика.

- **Особенности:**
  - Компактное хранение: сжатие данных.
  - Встроенные агрегации: `AVG`, `SUM`, `COUNT`, `MIN`, `MAX` за интервал.
  - Возможность создавать "агрегированные" временные ряды (например, усреднение за час).
  - Поддержка меток (tags) для фильтрации.

- **Когда использовать?**
  - Мониторинг: `cpu:server1`, `mem:server2`
  - Датчики IoT: `sensor:temp:01`, `sensor:pressure:02`
  - Аналитика: `requests:per_minute`, `errors:per_hour`

- **Команды:** `TS.ADD`, `TS.RANGE`, `TS.AGGREGATE`, `TS.CREATE`, `TS.MRANGE`

> ✅ **Пример:**  
> `TS.ADD temperature:room1 * 23.5`  
> `TS.RANGE temperature:room1 - +` → получить все значения за всё время

---

## 🧾 Сводная таблица типов

| Тип | Вид | Назначение | Пример использования |
|-----|-----|------------|----------------------|
| **String** | Встроенный | Кэш, счётчики | `SET session:123 "user1001"` |
| **Hash** | Встроенный | Объекты | `HSET user:1001 name "Иван" age "30"` |
| **List** | Встроенный | Очереди | `LPUSH queue:emails "email1@example.com"` |
| **Set** | Встроенный | Уникальные значения | `SADD tags:java "user1001"` |
| **Sorted Set** | Встроенный | Рейтинги | `ZADD leaderboard:user_score 1000 "user1001"` |
| **JSON** | Модуль | Сложные структуры | `JSON.SET user:1001 $ {"name": "Иван", "settings": {...}}` |
| **Stream** | Модуль | Логи, события | `XADD audit:log * action "login" user "1001"` |
| **TimeSeries** | Модуль | Метрики, датчики | `TS.ADD cpu:server1 * 85.2` |

---

## 💡 Как использовать модули?

- **RedisJSON, Streams, TimeSeries** — это **дополнительные модули**, которые нужно подключить к серверу Redis.
- Они **не включены в стандартный Redis**, но есть в:
  - **Redis Stack** — сборка Redis с модулями (https://redis.io/docs/stack/)
  - **Redis Cloud** — облачный Redis с поддержкой модулей
  - Можно подключить модули к локальному Redis (если вы используете бинарник с поддержкой модулей)

---