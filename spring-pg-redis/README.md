### Задача проекта java-bootcamp-redis

# 🚀 Проект: Spring Boot Java 21 — Сервис с PostgreSQL, Liquibase и Redis

## 🎯 Цель проекта

Разработать простой, но полноценный сервис на **Spring Boot (Java 21)**, чтобы освоить ключевые практики работы с базами данных, кэшированием и автоматизацией миграций — в реальных условиях разработки.

---

## 📚 Обучаемые компетенции

### 1. **Создание простого REST-сервиса на Spring Boot**
- Настроить проект с помощью [Spring Initializr](https://start.spring.io/).
- Определить основные зависимости: `Spring Web`, `Spring Data JPA`, `PostgreSQL Driver`, `Liquibase`, `Spring Data Redis`, `Validation`.
- Создать контроллеры (`@RestController`) с основными методами: `GET`, `POST`, `PUT`, `DELETE`.
- Научиться использовать `@RequestBody`, `@PathVariable`, `@RequestParam`, `@ResponseStatus`.
- Реализовать простую бизнес-логику в слое `@Service`.

### 2. **Работа с PostgreSQL через репозитории**
- Научиться создавать и использовать **Spring Data JPA Repository** для выполнения CRUD-операций.
- Понимать механизм автоматической генерации запросов по именам методов.
- Осваивать кастомные запросы через `@Query`, `@Modifying` и нативные SQL.
- Работать с транзакциями и связями между сущностями (`@OneToMany`, `@ManyToOne` и т.д.).

### 3. **Управление миграциями с Liquibase**
- Настроить **Liquibase** в проекте Spring Boot.
- Создавать и поддерживать читаемые, версионируемые миграции в формате `XML`, `YAML` или `SQL`.
- Использовать изменения: `createTable`, `addColumn`, `dropColumn`, `insert`, `customChange`.
- Понимать, как Liquibase отслеживает состояние БД через `DATABASECHANGELOG`.
- Избегать проблем с миграциями в командной разработке.

### 4. **Интеграция Redis и освоение ключевых паттернов**
- Подключить **Redis** через `spring-boot-starter-data-redis`.
- Использовать `RedisTemplate` и `StringRedisTemplate` для взаимодействия с Redis.
- Освоить основные паттерны:
  - **Cache-Aside (Cache-First)** — кэширование результатов запросов к БД.
  - **Cache Invalidation** — корректное удаление устаревших данных из кэша.
  - **Cache Warming** — предварительная загрузка популярных данных при старте сервиса.
  - **TTL (Time-To-Live)** — управление временем жизни кэша.
- Реализовать кэширование для часто запрашиваемых данных (например, список пользователей, справочники).
- Использовать Redis для хранения сессий, счетчиков, очередей (опционально).

### 5. **Практический стек разработки**
- Разработка локально с использованием **Docker** (PostgreSQL + Redis).
- Интеграция с **IntelliJ IDEA** — отладка, генерация кода, работа с репозиториями.
- Тестирование API через **Postman** (или VS Code + REST Client).
- Соблюдение принципа **Clean Architecture** — слои: Controller → Service → Repository → Entity.
- Выравнивание текста по значению для максимальной читаемости (как вы любите 😊).

---

## 🛠️ Формат проекта

- ✅ **Стартовый шаблон**: Spring Boot 3.x + Java 21
- ✅ **База данных**: PostgreSQL (в Docker)
- ✅ **Кэш**: Redis (в Docker)
- ✅ **Миграции**: Liquibase (в `src/main/resources/db/changelog/`)
- ✅ **Тестирование**: REST API через Postman
- ✅ **Логика**: Простой сущностный модель (например: `User`, `Product`, `Book`)
- ✅ **Документация**: Все шаги и решения — в этом `README.md`

---

## 💡 Рекомендации для успеха

- Начните с **одной сущности** — не пытайтесь сразу сделать всё.
- Сначала настройте **PostgreSQL + Liquibase** — убедитесь, что миграции работают.
- Затем добавьте **Redis** — реализуйте кэширование одного метода сервиса.
- Всегда проверяйте: *кэш работает? Данные обновляются при изменении в БД?*
- Используйте **рамки и выравнивание** — как вы привыкли. Это делает код и документацию удобной.

> 🌟 *Практика — лучший учитель. Каждый шаг — это не просто задача, а навык, который вы закрепите навсегда.*

---

## 🔗 Связанные знания (по вашим интересам)

- Использование `IF FOUND THEN` в PL/pgSQL → для сложной логики в триггерах/процедурах.
- Интеграция PostgreSQL с Java → через JPA/Hibernate.
- Spring Boot + Redis → через `@Cacheable`, `@CacheEvict`.