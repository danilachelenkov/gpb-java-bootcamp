# 🧠 Практикум по SOLID — задания от ментора

Я как ваш ментор подготовил для вас по 2–3 задания на каждый из пяти принципов SOLID, ориентированных на **Java 21** и **IntelliJ IDEA**. Все задания построены так, чтобы вы:

1. Сначала столкнулись с проблемой, вызванной нарушением принципа.  
2. Затем рефакторили код, применяя соответствующий принцип.  
3. В итоге увидели улучшения: **читаемость**, **тестируемость**, **гибкость**, **поддерживаемость**.

---

## 🧱 **S — Single Responsibility Principle**  
*Класс должен иметь одну и только одну причину для изменения.*

### **Задание S1: «Монолитный пользовательский сервис»**  
**Проблема:** У вас есть класс `UserService`, который:  
- регистрирует пользователей,  
- отправляет email-уведомления,  
- логирует действия в консоль.  

**Задача:**  
- Реализуйте такой «монолитный» `UserService`.  
- Затем разделите его на:  
  - `UserRegistrationService`  
  - `EmailNotificationService`  
  - `AuditLogger`  
- Убедитесь, что изменение формата лога или способа отправки email **не затрагивает** регистрацию.

### **Задание S2: «Форматтер + сохранение»**  
**Проблема:** Класс `ReportGenerator` форматирует отчёт в HTML и сохраняет его в файл.  

**Задача:**  
- Напишите такой класс.  
- Разделите обязанности:  
  - `HtmlReportFormatter` — только форматирует,  
  - `FileReportSaver` — только сохраняет.  
- Используйте **композицию** в клиентском коде.

---

## 🔁 **O — Open/Closed Principle**  
*Классы должны быть открыты для расширения и закрыты для модификации.*

### **Задание O1: «Расчёт скидок»**  
**Проблема:** Класс `DiscountCalculator` использует `if-else` для расчёта скидки в зависимости от типа клиента (`REGULAR`, `VIP`, `NEW`).  

**Задача:**  
- Напишите «наивную» реализацию с `if-else`.  
- Переделайте с использованием **стратегии**:  
  - создайте интерфейс `DiscountStrategy`,  
  - реализации: `VipDiscount`, `RegularDiscount`,  
  - фабрику: `DiscountStrategyFactory`.  
- Убедитесь, что добавление нового типа клиента **не требует правки** `DiscountCalculator`.

### **Задание O2: «Фильтрация продуктов»**  
**Проблема:** Класс `ProductFilter` имеет методы: `filterByColor`, `filterBySize`, `filterByColorAndSize`...

**Задача:**  
- Реализуйте «хардкодный» фильтр.  
- Замените его на систему с интерфейсом `Specification<T>` и методом `isSatisfied(T item)`.  
- Создайте:  
  - `ColorSpecification`,  
  - `SizeSpecification`,  
  - `AndSpecification`.  
- Покажите, как можно **комбинировать фильтры без изменения** класса фильтра.

---

## 🧬 **L — Liskov Substitution Principle**  
*Объекты в программе должны быть заменяемы их подтипами без изменения корректности программы.*

### **Задание L1: «Прямоугольник vs Квадрат»**  
**Проблема:** Класс `Square` наследуется от `Rectangle`, но нарушает LSP при изменении ширины/высоты.  

**Задача:**  
- Реализуйте `Rectangle` с `setWidth()` и `setHeight()`.  
- Создайте `Square extends Rectangle`, переопределив сеттеры так, чтобы ширина = высота.  
- Напишите тест, который **ломается при замене `Rectangle` на `Square`**.  
- Обсудите: почему наследование здесь — ошибка?  
- Предложите альтернативу (например, **композицию** или общий интерфейс `Shape`).

### **Задание L2: «Нарушение контракта в коллекции»**  
**Проблема:** Класс `ReadOnlyList` наследуется от `ArrayList`, но переопределяет `add()` так, чтобы он выбрасывал исключение.  

**Задача:**  
- Создайте такой класс.  
- Покажите, как клиентский код, ожидающий `List`, **ломается**.  
- Перепроектируйте:  
  - используйте `Collections.unmodifiableList()`,  
  - или создайте отдельный интерфейс `ReadableList` без методов модификации.

---

## 🔌 **I — Interface Segregation Principle**  
*Клиенты не должны зависеть от методов, которые они не используют.*

### **Задание I1: «Универсальный Worker-интерфейс»**  
**Проблема:** Интерфейс `Worker` содержит методы: `work()`, `eat()`, `sleep()`. Но `Robot` не ест и не спит.  

**Задача:**  
- Создайте такой интерфейс и классы `Human` и `Robot`.  
- Покажите, как `Robot` вынужден реализовывать ненужные методы (возможно, с `throw new UnsupportedOperationException()`).  
- Разбейте интерфейс на:  
  - `Workable`,  
  - `Eatable`,  
  - `Sleepable`.  
- Покажите, что `Robot` реализует **только `Workable`**.

### **Задание I2: «Сервис уведомлений»**  
**Проблема:** Интерфейс `NotificationService` содержит `sendEmail()`, `sendSms()`, `sendPush()`.  

**Задача:**  
- Создайте реализации `EmailService`, `SmsService` — но они вынуждены реализовывать все методы.  
- Разделите на:  
  - `EmailSender`,  
  - `SmsSender`,  
  - `PushSender`.  
- В клиентском коде используйте **только нужные зависимости** (например, через DI в Spring или вручную).

---

## 🧩 **D — Dependency Inversion Principle**  
*Модули верхнего уровня не должны зависеть от модулей нижнего уровня. Оба должны зависеть от абстракций. Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.*

### **Задание D1: «Жёсткая зависимость от базы данных»**  
**Проблема:** Класс `UserService` напрямую создаёт `PostgreSqlUserRepository`.  

**Задача:**  
- Напишите такую реализацию.  
- Введите интерфейс `UserRepository`.  
- Сделайте `PostgreSqlUserRepository` и, например, `InMemoryUserRepository` его реализациями.  
- Передавайте репозиторий в `UserService` **через конструктор**.  
- Покажите, как легко подменить реализацию — особенно в тестах.

### **Задание D2: «Уведомления без инверсии»**  
**Проблема:** `OrderService` напрямую использует `EmailNotificationService`.  

**Задача:**  
- Создайте такую прямую зависимость.  
- Введите интерфейс `NotificationService`.  
- Сделайте `EmailNotificationService` и `SmsNotificationService` его реализациями.  
- Используйте **фабрику** или **конфигурацию** (например, через `application.yaml` + Spring `@Qualifier`), чтобы выбирать реализацию.  

> 💡 **Подсказка:** вы можете смоделировать DI без Spring с помощью ручной передачи зависимостей — это усилит понимание.

---

## 🛠️ **Рекомендации по выполнению**

- Работайте в **IntelliJ IDEA** — используйте встроенные рефакторинги (`Extract Interface`, `Move`, и т.д.).  
- Пишите **юнит-тесты до и после рефакторинга** — это поможет убедиться, что поведение не сломано.  
- Обсуждайте:  
  - Почему код «до» был плохим?  
  - Что улучшилось «после»?  
- **Не используйте Spring на первых заданиях** — сначала поймите чистый ООП, потом интегрируйте с DI-контейнером.