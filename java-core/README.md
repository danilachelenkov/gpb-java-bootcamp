Чтобы выполнять задания по Java Core (включая работу с коллекциями, Stream API, многопоточностью, файлами, исключениями и т.д.), в IntelliJ IDEA тебе достаточно создать обычный Java-проект — ничего сложного не нужно.

### 🔧 Как создать подходящий проект в IntelliJ IDEA:

1. **File → New → Project**
2. В открывшемся окне:
   - **Project SDK**: убедись, что выбрана нужная версия JDK (рекомендуется JDK 17 или выше, особенно если будешь использовать современные фичи вроде `record`, `switch expressions` и т.д.).
   - **Project type**: выбери **"Empty Project"** или **"Java"** (в зависимости от версии IDEA).
   - **Не ставь галочку** на *"Create project from template"* — шаблоны типа Maven/Gradle пока не обязательны для Java Core.
3. Нажми **Next → Finish**.

✅ Всё! У тебя появится проект с папкой `src` — туда можно сразу создавать `.java` файлы и писать код.

📌 **Совет:**  
Создай в `src` пакет, например: `ru.kharin.core.stream`, и внутри него — классы для каждого задания (`Task1.java`, `Task2.java` и т.д.).  
Это поможет держать код в порядке и легко находить нужные задачи.