🧠 ** Общие рекомендации**

- Используйте записи (`record`) там, где уместно (например, для DTO).  
- Избегайте наследования там, где достаточно композиции (например, `FileService` использует `FileStorage`, а не наследует).  
- Всегда задавайте себе вопрос:  
  > «А что, если завтра появится ещё один тип X? Насколько легко будет его добавить?»

## 🔹 1. Инкапсуляция  
Скрытие внутреннего состояния объекта и предоставление контролируемого доступа к нему.

💡 **Цель:**  
Научиться защищать данные, избегать прямого доступа к полям, использовать геттеры/сеттеры с валидацией.

📝 **Задания:**

### Задание 1.1 — Банковский счёт  
Создайте класс `BankAccount`:

- **Приватные поля:** `balance` (`double`), `accountNumber` (`String`).
- **Публичные методы:** `deposit(double amount)`, `withdraw(double amount)`, `getBalance()`.

**Условия:**
- Нельзя снять больше, чем есть на счету.
- Нельзя внести отрицательную сумму.
- Номер счёта задаётся только при создании (только через конструктор).

💡 **Проблема:** Что будет, если сделать `balance` публичным? Почему это плохо?

---

### Задание 1.2 — Настройки приложения  
Создайте класс `AppConfig`:

- **Поля:** `maxRetries` (`int`), `timeoutMs` (`long`).

**Добавьте валидацию через сеттеры:**
- `maxRetries` ≥ 0 и ≤ 10
- `timeoutMs` ≥ 100 и ≤ 30_000

💡 **Проблема:** Как обеспечить, чтобы объект всегда был в валидном состоянии?

---

## 🔹 2. Наследование  
Создание новых классов на основе существующих для повторного использования кода.

💡 **Цель:**  
Понять, как строить иерархии классов, избегать "глубокого" наследования и дублирования.

📝 **Задания:**

### Задание 2.1 — Геометрические фигуры  
Создайте абстрактный класс `Shape` с методом `double getArea()`.

Реализуйте:
- `Circle` (радиус)
- `Rectangle` (ширина, высота)
- `Square` (сторона) — наследуйте от `Rectangle`

💡 **Проблема:** Почему `Square` не должен наследоваться от `Rectangle`, если менять ширину/высоту отдельно? (Это классическая проблема LSP — позже разберём.)

---

### Задание 2.2 — Система уведомлений  
Создайте абстрактный класс `Notification` с методом `send()`.

Реализуйте:
- `EmailNotification`
- `SmsNotification`
- `PushNotification`

У всех — разная логика отправки, но общая структура (например, поле `recipient`).

💡 **Проблема:** Что, если понадобится добавить `TelegramNotification`? Насколько легко расширять?

---

## 🔹 3. Полиморфизм  
Один интерфейс — множество реализаций. Возможность использовать объекты разных типов через общий интерфейс.

💡 **Цель:**  
Научиться писать гибкий код, не зависящий от конкретных реализаций.

📝 **Задания:**

### Задание 3.1 — Обработка платежей  
Создайте интерфейс `PaymentProcessor` с методом `processPayment(double amount)`.

Реализуйте:
- `CreditCardProcessor`
- `PayPalProcessor`
- `CryptoProcessor`

Создайте класс `PaymentService`, который принимает `PaymentProcessor` и вызывает `processPayment`.

💡 **Проблема:** Как добавить новую платёжную систему без изменения `PaymentService`?

---

### Задание 3.2 — Рисование фигур  
Используйте классы из **Задания 2.1**.  
Создайте список `List<Shape>`, добавьте туда круг, прямоугольник, квадрат.  
Пройдитесь по списку и выведите площадь каждой фигуры.

💡 **Проблема:** Почему мы можем вызывать `getArea()` у разных объектов через один тип `Shape`?

---

## 🔹 4. Абстракция  
Выделение существенных характеристик объекта и сокрытие деталей реализации.

💡 **Цель:**  
Научиться проектировать интерфейсы и абстрактные классы, фокусируясь на *что*, а не *как*.

📝 **Задания:**

### Задание 4.1 — Работа с файлами  
Создайте интерфейс `FileStorage`:

```java
void save(String filename, byte[] data);
byte[] read(String filename);
boolean exists(String filename);