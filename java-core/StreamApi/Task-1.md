## 📚 Набор задач для освоения Stream API

### Часть 1: 10 классических задач на основные операции Stream API  
Используй следующие методы:  
`filter`, `map`, `collect`, `flatMap`, `reduce`, `groupingBy`, `partitioningBy`, работа с `Optional`, `sorted`, `distinct`, `limit`, `skip` и др.

Задачи основаны на реалистичных коллекциях, часто встречающихся в enterprise-разработке:
- Списки объектов (`List<Person>`, `List<Order>`)
- Мапы (`Map<String, List<Order>>`)
- Числовые диапазоны
- Наборы строк

---

### Часть 2: 5 задач на параллельные потоки (`parallelStream()`)  
Включают вопросы:
- Производительности (когда `parallelStream()` действительно ускоряет выполнение)
- Корректности (работа с не-thread-safe коллекциями, порядком обработки и побочными эффектами)

Также основаны на типичных enterprise-данных: списках сущностей, агрегациях, фильтрациях больших объёмов данных.

----

💡 **Советы для тренировки:**

- Решайте сначала последовательно — убедитесь, что логика верна.  
- Потом перепишите через `parallelStream()` — проверьте, совпадает ли результат.  
- Обратите внимание: не все коллекторы потокобезопасны! Используйте `toConcurrentMap()`, `groupingByConcurrent` и т.д. при необходимости.  
- Избегайте побочных эффектов (`System.out.println`, изменение внешних переменных) в параллельных потоках.

----

## Данные для всех задач (можно использовать во всех):

```java
record Person(String name, int age, String city, double salary) {}
record Product(String name, String category, double price) {}
record Order(long id, String customerId, List<Product> products, boolean isCancelled) {}

List<Person> people = List.of(
    new Person("Alice", 28, "NYC", 75000.0),
    new Person("Bob", 35, "LA", 95000.0),
    new Person("Charlie", 22, "NYC", 45000.0),
    new Person("Diana", 40, "Chicago", 120000.0),
    new Person("Eve", 31, "NYC", 85000.0),
    new Person("Frank", 29, "LA", 70000.0)
);

List<Product> allProducts = List.of(
    new Product("Laptop", "Electronics", 1200.0),
    new Product("Phone", "Electronics", 800.0),
    new Product("Book", "Education", 20.0),
    new Product("Tablet", "Electronics", 500.0),
    new Product("Notebook", "Education", 5.0)
);

List<Order> orders = List.of(
    new Order(1L, "Alice", List.of(allProducts.get(0), allProducts.get(2)), false),
    new Order(2L, "Bob", List.of(allProducts.get(1), allProducts.get(3)), false),
    new Order(3L, "Charlie", List.of(allProducts.get(4)), true),
    new Order(4L, "Diana", List.of(allProducts.get(0), allProducts.get(1)), false),
    new Order(5L, "Eve", List.of(allProducts.get(2)), false)
);
```

----

### Часть 1: 10 классических задач на основные операции Stream API 

### Задача 1: Фильтрация и проекция  
**Постановка:** Получите имена всех людей из NYC.  
**Ожидаемый результат:** `["Alice", "Charlie", "Eve"]`  
**Методы:** `filter`, `map`, `collect`

---

### Задача 2: Сумма зарплат  
**Постановка:** Найдите общую сумму зарплат всех людей младше 35 лет.  
**Ожидаемый результат:** `75000.0 + 45000.0 + 70000.0 = 190000.0`  
**Методы:** `filter`, `mapToDouble`, `sum`


### Задача 3: Группировка  
**Постановка:** Сгруппируйте людей по городу.  
**Ожидаемый результат:**  

```java
{
  "NYC" = [Person("Alice", ...), Person("Charlie", ...), Person("Eve", ...)],
  "LA" = [Person("Bob", ...), Person("Frank", ...)],
  "Chicago" = [Person("Diana", ...)]
}
```
Методы: `groupingBy`


### Задача 4: Плоское отображение  
**Постановка:** Получите список всех продуктов из всех неотменённых заказов (без дубликатов).  
**Ожидаемый результат:** `[Laptop, Book, Phone, Tablet]` (в любом порядке)  
**Методы:** `filter`, `flatMap`, `map`, `distinct`, `collect`

---

### Задача 5: Сортировка и лимит  
**Постановка:** Найдите топ-3 самых дорогих людей по зарплате.  
**Ожидаемый результат:**

```java
[
  Person("Diana", 40, "Chicago", 120000.0),
  Person("Bob", 35, "LA", 95000.0),
  Person("Eve", 31, "NYC", 85000.0)
]
```

Методы: `sorted`, `limit`, `Comparator.comparing`


### Задача 6: Свёртка (`reduce`)  
**Постановка:** Найдите самого молодого человека (по возрасту).  
**Ожидаемый результат:** `Person("Charlie", 22, "NYC", 45000.0)`  
**Методы:** `reduce` (бинарный оператор)

---

### Задача 7: Разбиение (`partitioningBy`)  
**Постановка:** Разбейте людей на две группы: зарплата ≥ 80k и < 80k.  
**Ожидаемый результат:**


```java
{
  true = [Bob, Diana, Eve],
  false = [Alice, Charlie, Frank]
}
```

Методы: `partitioningBy`

### Задача 8: Преобразование в Set  
**Постановка:** Получите множество (`Set`) всех уникальных городов.  
**Ожидаемый результат:** `{"NYC", "LA", "Chicago"}`  
**Методы:** `map`, `collect(toSet())`

---

### Задача 9: Поиск первого подходящего  
**Постановка:** Найдите первого человека из LA старше 30 лет.  
**Ожидаемый результат:** `Optional[Person("Bob", 35, "LA", 95000.0)]`  
**Методы:** `filter`, `findFirst`

---

### Задача 10: Статистика по категориям  
**Постановка:** Для каждой категории товаров найдите среднюю цену.  
**Ожидаемый результат:**

```java
{
  "Electronics" = (1200 + 800 + 500) / 3 = 833.33...,
  "Education" = (20 + 5) / 2 = 12.5
}
```

## ⚡ Часть 2: Параллельные потоки (5 задач)

> **Важно:** все эти задачи должны использовать `.parallelStream()` вместо `.stream()`.  
> В реальных проектах `parallelStream` применяется для CPU-интенсивных операций над большими коллекциями.

---

### Задача 11: Параллельная сумма  
**Постановка:** Посчитайте сумму всех зарплат, используя параллельный поток.  
**Данные:** `people`  
**Ожидаемый результат:** `75000 + 95000 + 45000 + 120000 + 85000 + 70000 = 490000.0`  
**Методы:** `parallelStream()`, `mapToDouble`, `sum`  
✅ **Проверка:** результат должен совпадать с последовательным вариантом.

---

### Задача 12: Параллельная фильтрация  
**Постановка:** Найдите всех людей старше 30 лет с зарплатой > 80k, используя параллельный поток.  
**Ожидаемый результат:** `[Bob, Diana, Eve]`  
**Методы:** `parallelStream()`, `filter`, `collect`  
✅ **Проверка:** порядок может отличаться, но содержимое — то же.

---

### Задача 13: Параллельная группировка (осторожно!)  
**Постановка:** Сгруппируйте людей по городу с помощью `parallelStream()`.  
**Ожидаемый результат:** тот же, что в задаче 3.  
⚠️ **Важно:** убедитесь, что используется `Collectors.groupingByConcurrent`, если нужна корректность в параллельных потоках.  
✅ **Проверка:** результат может быть `ConcurrentMap`, но ключи и значения — те же.

---

### Задача 14: Параллельное преобразование с побочным эффектом  
**Постановка:** Попробуйте вывести в консоль имя каждого человека с помощью `parallelStream().forEach(System.out::println)`.  
**Ожидаемый результат:** все имена выведены, но в произвольном порядке.  
❗ **Это демонстрация недетерминированности `forEach` в параллельных потоках.**  
✅ **Вывод:** никогда не используйте `forEach` для последовательного вывода, если важен порядок!

---

### Задача 15: Параллельное сокращение с комбинатором  
**Постановка:** Найдите максимальную зарплату с помощью `parallelStream().reduce(...)`.  
**Ожидаемый результат:** `120000.0`  
**Методы:** `parallelStream()`, `mapToDouble`, `reduce(Double::max)`  
✅ **Проверка:** результат совпадает с последовательным.


